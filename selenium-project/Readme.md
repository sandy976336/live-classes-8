After this you can build the project using maven and get the jar file -:

mvn clean install

#After this maven would compile the code and based on your code and dependencies would build the jar file for you.